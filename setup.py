from setuptools import setup, find_packages

setup(
    name="backbone_pyspark_deploy",
    verion="0.1",
    packages=find_packages(),
    description="DS project deployement - BackBone",
    author="Giulia Bianchi",
    author_email="giulbia@gmail.com",
    licence="",
    install_requires=[],
    tests_requires=["pytest"],
    scripts=[],
    include_package_data=False,
    zip_safe=False
)
